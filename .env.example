# Copy this to .env and fill in:
#   cp .env.example .env

# Your miner private key. NEVER commit the real .env file.
PRIVATE_KEY=0xYOUR_PRIVATE_KEY_HERE_64_HEX_CHARS

# Base mainnet RPC. Public node works; for serious mining use Alchemy/QuickNode/Helius.
RPC_URL=https://mainnet.base.org

# Optional: explicit thread/batch count (0 = auto-detect cores; GPU = batch size).
# MINER_THREADS=4

# Optional: Telegram notifications on hit / tx sent / mined / revert.
# Create a bot via @BotFather → put the token below.
# Find your chat_id by messaging @userinfobot.
# TG_BOT_TOKEN=
# TG_CHAT_ID=
